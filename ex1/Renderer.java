/** Renderer interface for displaying a Tic-Tac-Toe board;
 * empty implementations perform no rendering.
 *
 * @author Amit Moses
 */

interface Renderer {
    void renderBoard(Board board);
}
